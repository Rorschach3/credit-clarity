---
active: true
iteration: 1
max_iterations: 0
completion_promise: null
started_at: "2026-01-11T02:14:56Z"
---

Extract tradelines from a credit report PDF with OCR + AI, reliably --input backend/TransUnion-06-10-2025.pdf --output-json ./backend/results/extracted_tradelines.json --output-debug-dir ./backend/results/debug --goal Return a normalized array of tradelines with creditor_name, account_number_masked, account_type, account_status, date_opened, credit_bureau, credit_limit_cents, monthly_payment_cents, is_negative, confidence per field, and source_refs per field. --constraints Never hallucinate fields. If not present, set null. Mask account numbers except last 4. Preserve page references for every extracted value. --loop.max-attempts 6 --loop.stop-when quality.score >= 0.92 AND quality.missing_required <= 1 AND quality.cross_page_conflicts == 0 --loop.on-fail escalate --pipeline 1) PDF->Images: render each page at 300dpi (fallback 450dpi if OCR confidence < 0.80). 2) Preprocess: deskew + denoise + contrast normalize; run both original and preprocessed if needed. 3) OCR Pass A (fast): extract text per page + per region; capture word-level confidence + bounding boxes. 4) OCR Pass B (targeted): detect tradeline-like blocks using anchors (e.g., 'Account Name', 'Creditor', 'Account Type', 'Account Status', 'Balance', 'Credit Limit', 'Monthly Payment', 'Date Opened', 'Remarks'). 5) Heuristic Parse: attempt regex/block parsing into candidate tradelines; record parse gaps. 6) AI Extract: send only relevant page slices (and OCR text) to LLM with schema + examples; require citations to page+line/box ids. 7) Merge: reconcile heuristic + AI outputs; prefer fields with higher confidence and stronger citations. 8) Validate: run schema validation, field normalizers (dates/currency), and invariants (money numeric, dates plausible). 9) Score: compute quality.score using coverage + confidence + consistency + citations completeness. --ai.model gpt-5-mini --ai.schema Tradeline[] --ai.prompt You are extracting tradelines from OCR text that may be noisy. Output JSON ONLY: an array of Tradeline objects. Rules: - Do not invent values. If not present, output null. - Mask account numbers: keep only last 4 digits (e.g., "****1234"). - Every non-null field must include source_refs: [{page: number, evidence: string}]. - If conflicting values exist, set the field to null and include conflicts[] with candidates + evidence. --qc.required-fields creditor_name,account_status,account_type --qc.optional-fields date_opened,credit_bureau,credit_limit_cents,monthly_payment_cents,is_negative,derogatory_flags,account_balance_cents,remarks,payment_history --qc.normalization dates=ISO8601,currency=integer_cents,booleans=strict --debug.save ocr_text,ocr_boxes,page_images,ai_requests,ai_responses,merged_result,validation_report --report print_summary
